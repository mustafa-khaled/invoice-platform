
NEXT_PUBLIC_APP_URL=
NODE_ENV=development

# App secrets
AUTH_SECRET=

# Mail (Mailtrap) - replace USERNAME and PASSWORD with the values shown in your Mailtrap SMTP credentials
EMAIL_SERVER_HOST=      # or the exact host from Mailtrap (e.g. live.smtp.mailtrap.io)
EMAIL_SERVER_PORT=                   # or 587, 465, 25 — must be a number
EMAIL_SERVER_USER=
EMAIL_SERVER_PASSWORD=
EMAIL_FROM=

MAILTRAP_TOKEN=

# Database
DATABASE_URL=